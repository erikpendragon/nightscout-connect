#!/usr/bin/env node

// must load before anything that can log
require('../lib/log-scrub').install( );

var argv = require('yargs')
  .scriptName('nightscout-connect')
  .usage('$0 <cmd> [args]')
  .env("CONNECT")
  .commandDir('../commands')
  .command(require('../demo'))
  .help( )
  .completion( )
  .argv;

